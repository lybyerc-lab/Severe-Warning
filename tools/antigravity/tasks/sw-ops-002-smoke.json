{
  "version": "SW_ANTIGRAVITY_WORKER_TASK_V1",
  "taskId": "SW-OPS-002-SMOKE",
  "repository": "lybyerc-lab/Severe-Warning",
  "repositoryUrl": "https://github.com/lybyerc-lab/Severe-Warning",
  "exactBaseSha": "10ccd2723b8fa6b925f47629491a9e51e6388500",
  "goal": "After verifying the exact base checkout, create exactly one new file at tools/antigravity/fixtures/sw-ops-002-smoke.txt with exactly two lines: first line 'SW-OPS-002 sandbox worker reached' and second line 'GitHub writes remain Director-controlled'. Make no other repository edits. Then produce the required patch and structured result files.",
  "nonGoals": [
    "No changes outside the single allowed fixture file",
    "No dependency installation",
    "No GitHub writes or credentials",
    "No commit, push, PR, merge, release, deployment, or publication",
    "No gameplay, runtime, Android, art, presentation, campaign, scoring, input, camera, or progression changes"
  ],
  "allowedPaths": [
    "tools/antigravity/fixtures/sw-ops-002-smoke.txt"
  ],
  "protectedPaths": [
    "runtime/",
    "modern/",
    "MechanicsLab/",
    "android/",
    "patches/"
  ],
  "proofPlan": [
    "Record git rev-parse HEAD after checkout and require it to equal the exact base SHA",
    "Create only the single allowed fixture file with the exact requested two-line content",
    "Run git status --short and prove no other repository path changed",
    "Run git diff --check and require success",
    "Create worker.patch from git diff --binary --no-ext-diff against the exact base",
    "Create result.json with changedFiles containing only the allowed fixture path"
  ],
  "requestedTests": [
    "git status --short",
    "git diff --check"
  ],
  "tokenBudget": 8000,
  "maxPatchBytes": 10000,
  "requirePatch": true,
  "agent": "antigravity-preview-05-2026"
}
