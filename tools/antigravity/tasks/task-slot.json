{
  "version": "SW_ANTIGRAVITY_WORKER_TASK_V1",
  "taskId": "SW-AG-ART-009-CI-GATE-004",
  "repository": "lybyerc-lab/Severe-Warning",
  "repositoryUrl": "https://github.com/lybyerc-lab/Severe-Warning",
  "exactBaseSha": "19a83af9f77d51feab64dc0edfe94c896b9b5c17",
  "goal": "Use code_execution immediately. Perform exactly one bounded workflow edit in /workspace/repo/.github/workflows/sw-art-009-cow17-runtime.yml. Do not change branches or Git history. Do not clone, fetch, search for, or inspect any other repository copy. The Director has already made the verification decision. Inside the 'Verify protected Cow seam and build' run block, replace the single line 'pnpm run verify:v500' with exactly these two lines at the same indentation: 'pnpm run verify:v510' followed immediately by 'pnpm run verify:phase4'. Do not alter any other line, ordering, trigger, gate, proof, comment, whitespace outside that replacement, or file. Re-read the edited workflow, run the requested lightweight checks, and stop.",
  "nonGoals": [
    "No runtime, gameplay, campaign, Cow behavior, art, asset, or presentation changes",
    "No changes to source assembly order or worker scope allowlist",
    "No deletion or skipping of Cow 17 task-specific static, build, asset identity, or browser proof",
    "No package.json or verifier-script edits",
    "No GitHub credentials or writes",
    "No commit, push, PR, merge, release, deployment, or canonical promotion"
  ],
  "allowedPaths": [
    ".github/workflows/sw-art-009-cow17-runtime.yml"
  ],
  "protectedPaths": [
    "runtime/",
    "assets/",
    "scripts/",
    "MechanicsLab/",
    "modern/",
    "android/",
    "package.json",
    "pnpm-lock.yaml",
    "tools/"
  ],
  "proofPlan": [
    "Confirm the editable workflow contains exactly one pnpm run verify:v500 line",
    "Replace only that line with pnpm run verify:v510 then pnpm run verify:phase4",
    "Confirm verify:process remains immediately before the replacement and verify:asset-pipeline remains after it",
    "Run the focused requestedTests",
    "Host extracts only the allowlisted workflow file and validates the patch against exactBaseSha with git apply --check and git diff --check"
  ],
  "requestedTests": [
    "grep -n 'pnpm run verify:process\\|pnpm run verify:v500\\|pnpm run verify:v510\\|pnpm run verify:phase4\\|pnpm run verify:asset-pipeline' /workspace/repo/.github/workflows/sw-art-009-cow17-runtime.yml",
    "test \"$(grep -c 'pnpm run verify:v500' /workspace/repo/.github/workflows/sw-art-009-cow17-runtime.yml)\" = 0",
    "test \"$(grep -c 'pnpm run verify:v510' /workspace/repo/.github/workflows/sw-art-009-cow17-runtime.yml)\" = 1",
    "test \"$(grep -c 'pnpm run verify:phase4' /workspace/repo/.github/workflows/sw-art-009-cow17-runtime.yml)\" = 1"
  ],
  "tokenBudget": 30000,
  "continuationTokenBudget": 30000,
  "maxContinuationAttempts": 1,
  "maxPatchBytes": 12000,
  "requirePatch": true,
  "agent": "antigravity-preview-05-2026",
  "sourceMode": "focused-inline",
  "contextPaths": []
}
